<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>PILE STUDIO | Intentional Design Objects</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;0,500;0,600;1,300;1,400&family=DM+Sans:wght@200;300;400;500&family=Bebas+Neue&display=swap" rel="stylesheet">
<style>
  *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

  :root {
    --white: #FAFAF8;
    --off-white: #F4F2EE;
    --cream: #EDE9E1;
    --warm-gray: #C8C4BC;
    --mid-gray: #8A8680;
    --dark: #1A1814;
    --accent: #B87A4A;
    --accent-light: #D4A574;
    --text: #2C2A26;
    --border: rgba(0,0,0,0.08);
  }

  html { scroll-behavior: smooth; }

  body {
    font-family: 'DM Sans', sans-serif;
    background: var(--white);
    color: var(--text);
    overflow-x: hidden;
    cursor: none;
  }

  /* CUSTOM CURSOR */
  .cursor {
    width: 12px; height: 12px;
    background: var(--dark);
    border-radius: 50%;
    position: fixed;
    top: 0; left: 0;
    pointer-events: none;
    z-index: 9999;
    transition: transform 0.1s ease;
    mix-blend-mode: multiply;
  }
  .cursor-ring {
    width: 36px; height: 36px;
    border: 1px solid var(--dark);
    border-radius: 50%;
    position: fixed;
    top: 0; left: 0;
    pointer-events: none;
    z-index: 9998;
    transition: transform 0.25s ease, width 0.3s, height 0.3s, border-color 0.3s;
    opacity: 0.5;
  }
  .cursor-ring.expand { width: 60px; height: 60px; opacity: 0.3; border-color: var(--accent); }

  /* ANNOUNCEMENT BAR */
  .announcement {
    background: var(--dark);
    color: var(--cream);
    text-align: center;
    padding: 10px 20px;
    font-size: 11px;
    letter-spacing: 2.5px;
    text-transform: uppercase;
    font-weight: 300;
    position: relative;
    overflow: hidden;
  }
  .announcement-ticker {
    display: inline-block;
    animation: ticker 18s linear infinite;
    white-space: nowrap;
  }
  @keyframes ticker {
    0% { transform: translateX(100vw); }
    100% { transform: translateX(-100%); }
  }

  /* NAV */
  nav {
    position: sticky;
    top: 0;
    z-index: 1000;
    background: rgba(250,250,248,0.92);
    backdrop-filter: blur(20px);
    border-bottom: 1px solid var(--border);
    padding: 0 48px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 70px;
    transition: box-shadow 0.3s;
  }
  nav.scrolled { box-shadow: 0 4px 40px rgba(0,0,0,0.06); }

  .nav-logo {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 26px;
    letter-spacing: 4px;
    color: var(--dark);
    text-decoration: none;
    position: relative;
  }
  .nav-logo::after {
    content: '®';
    font-size: 8px;
    position: absolute;
    top: 4px; right: -10px;
    font-family: 'DM Sans', sans-serif;
  }

  .nav-links {
    display: flex;
    gap: 36px;
    list-style: none;
  }
  .nav-links a {
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    text-decoration: none;
    color: var(--text);
    font-weight: 400;
    position: relative;
    padding-bottom: 2px;
    transition: color 0.3s;
  }
  .nav-links a::after {
    content: '';
    position: absolute;
    bottom: 0; left: 0;
    width: 0; height: 1px;
    background: var(--accent);
    transition: width 0.3s ease;
  }
  .nav-links a:hover { color: var(--accent); }
  .nav-links a:hover::after { width: 100%; }

  .nav-actions { display: flex; gap: 20px; align-items: center; }
  .nav-icon-btn {
    background: none; border: none; cursor: none;
    font-size: 18px; color: var(--text);
    position: relative;
    transition: color 0.3s, transform 0.2s;
  }
  .nav-icon-btn:hover { color: var(--accent); transform: scale(1.1); }
  .cart-badge {
    position: absolute; top: -6px; right: -6px;
    background: var(--accent); color: white;
    border-radius: 50%; width: 16px; height: 16px;
    font-size: 9px; display: flex; align-items: center; justify-content: center;
    font-weight: 500;
  }

  /* HERO */
  .hero {
    height: 95vh;
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
    background: var(--cream);
  }
  .hero-bg {
    position: absolute; inset: 0;
    background: 
      radial-gradient(ellipse 60% 70% at 70% 40%, rgba(184,122,74,0.12) 0%, transparent 60%),
      radial-gradient(ellipse 50% 60% at 20% 80%, rgba(200,196,188,0.3) 0%, transparent 50%),
      var(--cream);
  }
  .hero-noise {
    position: absolute; inset: 0; opacity: 0.025;
    background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='noise'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23noise)'/%3E%3C/svg%3E");
  }

  .hero-content {
    position: relative; z-index: 2;
    text-align: center;
    max-width: 900px;
    padding: 0 40px;
  }
  .hero-eyebrow {
    font-size: 10px;
    letter-spacing: 5px;
    text-transform: uppercase;
    color: var(--accent);
    font-weight: 400;
    margin-bottom: 28px;
    opacity: 0;
    animation: fadeUp 0.8s ease 0.3s forwards;
  }
  .hero-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(64px, 10vw, 120px);
    font-weight: 300;
    line-height: 0.9;
    color: var(--dark);
    margin-bottom: 32px;
    opacity: 0;
    animation: fadeUp 0.9s ease 0.5s forwards;
  }
  .hero-title em {
    font-style: italic;
    color: var(--accent);
  }
  .hero-sub {
    font-size: 14px;
    color: var(--mid-gray);
    letter-spacing: 1.5px;
    font-weight: 300;
    margin-bottom: 48px;
    opacity: 0;
    animation: fadeUp 0.9s ease 0.7s forwards;
  }
  .hero-cta {
    display: inline-flex;
    gap: 16px;
    opacity: 0;
    animation: fadeUp 0.9s ease 0.9s forwards;
  }
  .btn-primary {
    background: var(--dark);
    color: var(--white);
    padding: 14px 36px;
    font-size: 11px;
    letter-spacing: 3px;
    text-transform: uppercase;
    text-decoration: none;
    font-weight: 400;
    transition: background 0.3s, transform 0.2s;
    display: inline-block;
    cursor: none;
  }
  .btn-primary:hover { background: var(--accent); transform: translateY(-2px); }
  .btn-outline {
    border: 1px solid var(--dark);
    color: var(--dark);
    padding: 14px 36px;
    font-size: 11px;
    letter-spacing: 3px;
    text-transform: uppercase;
    text-decoration: none;
    font-weight: 400;
    transition: all 0.3s;
    display: inline-block;
    cursor: none;
  }
  .btn-outline:hover { background: var(--dark); color: var(--white); transform: translateY(-2px); }

  .hero-scroll {
    position: absolute;
    bottom: 36px; left: 50%;
    transform: translateX(-50%);
    display: flex; flex-direction: column; align-items: center; gap: 10px;
    opacity: 0;
    animation: fadeUp 0.9s ease 1.2s forwards;
  }
  .hero-scroll span {
    font-size: 9px; letter-spacing: 3px; text-transform: uppercase;
    color: var(--mid-gray); font-weight: 300;
  }
  .scroll-line {
    width: 1px; height: 48px;
    background: linear-gradient(to bottom, var(--accent), transparent);
    animation: scrollPulse 2s ease infinite;
  }
  @keyframes scrollPulse {
    0%, 100% { opacity: 1; transform: scaleY(1); }
    50% { opacity: 0.3; transform: scaleY(0.6); }
  }

  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(24px); }
    to { opacity: 1; transform: translateY(0); }
  }

  /* MARQUEE STRIP */
  .marquee-strip {
    background: var(--dark);
    padding: 14px 0;
    overflow: hidden;
    display: flex;
  }
  .marquee-track {
    display: flex;
    gap: 60px;
    animation: marquee 20s linear infinite;
    white-space: nowrap;
  }
  .marquee-track span {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 14px;
    letter-spacing: 4px;
    color: var(--cream);
    opacity: 0.7;
  }
  .marquee-track .dot { color: var(--accent); opacity: 1; }
  @keyframes marquee {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  /* SECTION COMMON */
  section { padding: 100px 48px; }
  .section-label {
    font-size: 9px; letter-spacing: 5px; text-transform: uppercase;
    color: var(--accent); font-weight: 400; margin-bottom: 16px;
    display: flex; align-items: center; gap: 12px;
  }
  .section-label::after {
    content: ''; flex: 1; max-width: 60px;
    height: 1px; background: var(--accent); opacity: 0.4;
  }
  .section-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(40px, 5vw, 68px);
    font-weight: 300;
    line-height: 1.05;
    color: var(--dark);
    margin-bottom: 24px;
  }
  .section-title em { font-style: italic; color: var(--accent); }

  /* COLLECTIONS GRID */
  .collections { background: var(--white); }
  .collections-header {
    display: flex; justify-content: space-between; align-items: flex-end;
    margin-bottom: 56px;
  }
  .collections-grid {
    display: grid;
    grid-template-columns: 1.4fr 1fr 1fr;
    gap: 20px;
  }
  .collection-card {
    position: relative; overflow: hidden;
    cursor: none;
    background: var(--off-white);
  }
  .collection-card:first-child { grid-row: span 2; }
  .collection-img {
    width: 100%; aspect-ratio: 4/5;
    object-fit: cover;
    transition: transform 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: block;
    background: var(--cream);
  }
  .collection-card:first-child .collection-img { aspect-ratio: unset; height: 100%; }
  .collection-card:hover .collection-img { transform: scale(1.06); }
  .collection-overlay {
    position: absolute; inset: 0;
    background: linear-gradient(to top, rgba(26,24,20,0.65) 0%, transparent 50%);
    display: flex; flex-direction: column; justify-content: flex-end;
    padding: 32px;
    transition: background 0.4s;
  }
  .collection-card:hover .collection-overlay {
    background: linear-gradient(to top, rgba(26,24,20,0.75) 0%, rgba(26,24,20,0.1) 60%);
  }
  .collection-name {
    font-family: 'Cormorant Garamond', serif;
    font-size: 28px; font-weight: 400;
    color: white; line-height: 1;
    margin-bottom: 10px;
  }
  .collection-link {
    font-size: 10px; letter-spacing: 3px; text-transform: uppercase;
    color: rgba(255,255,255,0.7); text-decoration: none;
    display: flex; align-items: center; gap: 8px;
    transition: gap 0.3s, color 0.3s;
  }
  .collection-card:hover .collection-link { gap: 14px; color: var(--accent-light); }

  /* PRODUCT GRID */
  .products { background: var(--off-white); }
  .product-filters {
    display: flex; gap: 8px; margin-bottom: 48px; flex-wrap: wrap;
  }
  .filter-btn {
    padding: 8px 20px;
    font-size: 10px; letter-spacing: 2px; text-transform: uppercase;
    border: 1px solid var(--border);
    background: transparent; cursor: none;
    color: var(--mid-gray);
    transition: all 0.25s;
    font-family: 'DM Sans', sans-serif;
  }
  .filter-btn.active, .filter-btn:hover {
    background: var(--dark); color: var(--white); border-color: var(--dark);
  }
  .product-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 24px;
  }
  .product-card {
    background: var(--white);
    cursor: none;
    position: relative;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
    opacity: 0;
    transform: translateY(30px);
  }
  .product-card.visible {
    opacity: 1; transform: translateY(0);
    transition: opacity 0.6s ease, transform 0.6s ease, box-shadow 0.3s, transform 0.3s;
  }
  .product-card:hover {
    transform: translateY(-6px);
    box-shadow: 0 20px 60px rgba(0,0,0,0.1);
  }
  .product-img-wrap {
    position: relative; overflow: hidden;
    aspect-ratio: 3/4;
    background: var(--off-white);
  }
  .product-img {
    width: 100%; height: 100%;
    object-fit: cover;
    display: flex; align-items: center; justify-content: center;
    font-size: 60px;
    transition: transform 0.6s ease;
    background: var(--cream);
  }
  .product-card:hover .product-img { transform: scale(1.04); }
  .product-badge {
    position: absolute; top: 14px; left: 14px;
    background: var(--accent); color: white;
    font-size: 9px; letter-spacing: 2px; text-transform: uppercase;
    padding: 4px 10px;
  }
  .product-quick-add {
    position: absolute; bottom: 0; left: 0; right: 0;
    background: var(--dark);
    color: var(--white);
    text-align: center;
    padding: 13px;
    font-size: 10px; letter-spacing: 2.5px; text-transform: uppercase;
    transform: translateY(100%);
    transition: transform 0.3s ease;
    cursor: none;
    border: none; width: 100%;
    font-family: 'DM Sans', sans-serif;
  }
  .product-quick-add:hover { background: var(--accent); }
  .product-card:hover .product-quick-add { transform: translateY(0); }
  .product-wishlist {
    position: absolute; top: 14px; right: 14px;
    width: 32px; height: 32px;
    background: white;
    border: none; border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 14px; cursor: none;
    opacity: 0; transform: scale(0.8);
    transition: opacity 0.3s, transform 0.3s;
  }
  .product-card:hover .product-wishlist { opacity: 1; transform: scale(1); }
  .product-info { padding: 18px 18px 20px; }
  .product-name {
    font-size: 12px; letter-spacing: 1.5px; text-transform: uppercase;
    color: var(--dark); font-weight: 400; margin-bottom: 8px;
  }
  .product-price { font-size: 13px; color: var(--mid-gray); }
  .product-price .sale { color: var(--accent); font-weight: 500; margin-right: 8px; }
  .product-price .original { text-decoration: line-through; font-size: 11px; }

  /* DAY/NIGHT TOGGLE SECTION */
  .mood-section {
    background: var(--dark);
    padding: 100px 48px;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 80px;
    align-items: center;
  }
  .mood-text { color: var(--white); }
  .mood-text .section-label { color: var(--accent-light); }
  .mood-text .section-label::after { background: var(--accent-light); }
  .mood-text .section-title { color: var(--white); }
  .mood-text p { color: rgba(255,255,255,0.5); font-size: 14px; line-height: 1.8; margin-bottom: 36px; font-weight: 300; }
  .mood-toggle-wrap { display: flex; gap: 12px; margin-bottom: 36px; }
  .mood-btn {
    padding: 10px 24px;
    font-size: 10px; letter-spacing: 2px; text-transform: uppercase;
    border: 1px solid rgba(255,255,255,0.2);
    background: transparent; color: rgba(255,255,255,0.5);
    cursor: none; transition: all 0.3s;
    font-family: 'DM Sans', sans-serif;
  }
  .mood-btn.active { background: var(--accent); border-color: var(--accent); color: white; }
  .mood-img-wrap {
    position: relative; aspect-ratio: 4/5;
    overflow: hidden;
  }
  .mood-img {
    position: absolute; inset: 0;
    width: 100%; height: 100%;
    object-fit: cover;
    background: var(--cream);
    display: flex; align-items: center; justify-content: center;
    font-size: 100px;
    transition: opacity 0.8s ease;
  }
  .mood-img.day { background: linear-gradient(135deg, #F5E6D3, #EDE9E1); }
  .mood-img.night { background: linear-gradient(135deg, #1A1814, #2C2820); opacity: 0; }
  .mood-img.night.show { opacity: 1; }
  .mood-img.day.hide { opacity: 0; }

  /* ABOUT / MANIFESTO */
  .manifesto {
    background: var(--cream);
    padding: 140px 48px;
    text-align: center;
    position: relative;
    overflow: hidden;
  }
  .manifesto-bg-text {
    position: absolute;
    font-family: 'Bebas Neue', sans-serif;
    font-size: 30vw;
    color: rgba(0,0,0,0.03);
    top: 50%; left: 50%;
    transform: translate(-50%, -50%);
    white-space: nowrap;
    pointer-events: none;
    letter-spacing: -5px;
  }
  .manifesto-content { position: relative; z-index: 1; max-width: 780px; margin: 0 auto; }
  .manifesto blockquote {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(28px, 4vw, 48px);
    font-weight: 300;
    font-style: italic;
    line-height: 1.3;
    color: var(--dark);
    margin-bottom: 32px;
  }
  .manifesto cite {
    font-size: 11px; letter-spacing: 3px; text-transform: uppercase;
    color: var(--accent); font-style: normal;
  }

  /* PARALLAX BANNER */
  .banner {
    position: relative; height: 70vh;
    overflow: hidden;
    display: flex; align-items: center; justify-content: center;
    background: var(--cream);
  }
  .banner-inner {
    position: relative; z-index: 2;
    text-align: center;
  }
  .banner-bg {
    position: absolute; inset: -20%;
    background: linear-gradient(135deg, var(--cream) 0%, #E8E2D8 50%, var(--cream) 100%);
    transform: translateY(0);
    transition: transform 0.1s linear;
  }
  .banner-eyebrow {
    font-size: 10px; letter-spacing: 5px; text-transform: uppercase;
    color: var(--accent); margin-bottom: 20px;
  }
  .banner-title {
    font-family: 'Cormorant Garamond', serif;
    font-size: clamp(48px, 7vw, 88px);
    font-weight: 300; color: var(--dark); line-height: 1;
    margin-bottom: 24px;
  }
  .banner-sub {
    font-size: 13px; color: var(--mid-gray); margin-bottom: 36px;
    max-width: 400px; margin-left: auto; margin-right: auto; line-height: 1.7;
  }

  /* TESTIMONIALS */
  .testimonials { background: var(--white); padding: 100px 48px; }
  .testimonials-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
    margin-top: 56px;
  }
  .testimonial-card {
    background: var(--off-white);
    padding: 36px;
    position: relative;
    transition: transform 0.3s, box-shadow 0.3s;
  }
  .testimonial-card:hover { transform: translateY(-4px); box-shadow: 0 16px 48px rgba(0,0,0,0.08); }
  .stars { color: var(--accent); font-size: 13px; margin-bottom: 20px; letter-spacing: 3px; }
  .testimonial-text {
    font-family: 'Cormorant Garamond', serif;
    font-size: 18px; font-style: italic; font-weight: 300;
    line-height: 1.6; color: var(--dark); margin-bottom: 24px;
  }
  .testimonial-author { font-size: 11px; letter-spacing: 2px; text-transform: uppercase; color: var(--mid-gray); }

  /* NEWSLETTER */
  .newsletter {
    background: var(--dark);
    padding: 100px 48px;
    text-align: center;
  }
  .newsletter .section-label { justify-content: center; }
  .newsletter .section-label::after { display: none; }
  .newsletter .section-title { color: var(--white); margin-bottom: 16px; }
  .newsletter p { color: rgba(255,255,255,0.45); font-size: 14px; font-weight: 300; margin-bottom: 44px; }
  .newsletter-form {
    display: flex;
    max-width: 480px;
    margin: 0 auto;
    border: 1px solid rgba(255,255,255,0.15);
    overflow: hidden;
    transition: border-color 0.3s;
  }
  .newsletter-form:focus-within { border-color: var(--accent); }
  .newsletter-form input {
    flex: 1; padding: 16px 20px;
    background: transparent;
    border: none; outline: none;
    color: white;
    font-size: 13px;
    font-family: 'DM Sans', sans-serif;
  }
  .newsletter-form input::placeholder { color: rgba(255,255,255,0.3); }
  .newsletter-form button {
    padding: 16px 28px;
    background: var(--accent);
    border: none; cursor: none;
    color: white;
    font-size: 10px; letter-spacing: 2.5px; text-transform: uppercase;
    font-family: 'DM Sans', sans-serif;
    transition: background 0.3s;
    white-space: nowrap;
  }
  .newsletter-form button:hover { background: var(--accent-light); }

  /* FOOTER */
  footer {
    background: var(--off-white);
    border-top: 1px solid var(--border);
    padding: 80px 48px 40px;
  }
  .footer-top {
    display: grid;
    grid-template-columns: 2fr 1fr 1fr 1fr;
    gap: 60px;
    margin-bottom: 60px;
  }
  .footer-brand-name {
    font-family: 'Bebas Neue', sans-serif;
    font-size: 32px; letter-spacing: 5px;
    color: var(--dark); margin-bottom: 16px;
  }
  .footer-tagline { font-size: 13px; color: var(--mid-gray); line-height: 1.8; font-weight: 300; margin-bottom: 28px; }
  .footer-social { display: flex; gap: 14px; }
  .social-link {
    width: 36px; height: 36px;
    border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    text-decoration: none; font-size: 14px;
    color: var(--mid-gray);
    transition: all 0.25s;
    cursor: none;
  }
  .social-link:hover { background: var(--dark); color: white; border-color: var(--dark); }
  .footer-col h4 {
    font-size: 10px; letter-spacing: 3px; text-transform: uppercase;
    color: var(--dark); font-weight: 500;
    margin-bottom: 20px;
  }
  .footer-col ul { list-style: none; }
  .footer-col ul li { margin-bottom: 10px; }
  .footer-col ul a {
    font-size: 13px; color: var(--mid-gray); text-decoration: none; font-weight: 300;
    transition: color 0.2s;
  }
  .footer-col ul a:hover { color: var(--accent); }
  .footer-bottom {
    border-top: 1px solid var(--border);
    padding-top: 28px;
    display: flex; justify-content: space-between; align-items: center;
  }
  .footer-copy { font-size: 11px; color: var(--mid-gray); letter-spacing: 1px; }
  .footer-legal { display: flex; gap: 20px; }
  .footer-legal a { font-size: 11px; color: var(--mid-gray); text-decoration: none; }
  .footer-legal a:hover { color: var(--accent); }

  /* CART DRAWER */
  .cart-overlay {
    position: fixed; inset: 0;
    background: rgba(0,0,0,0.3);
    z-index: 2000;
    opacity: 0; pointer-events: none;
    transition: opacity 0.3s;
    backdrop-filter: blur(4px);
  }
  .cart-overlay.open { opacity: 1; pointer-events: all; }
  .cart-drawer {
    position: fixed; top: 0; right: 0; bottom: 0;
    width: 380px;
    background: var(--white);
    z-index: 2001;
    transform: translateX(100%);
    transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
    display: flex; flex-direction: column;
  }
  .cart-drawer.open { transform: translateX(0); }
  .cart-header {
    padding: 28px 32px;
    border-bottom: 1px solid var(--border);
    display: flex; justify-content: space-between; align-items: center;
  }
  .cart-header h3 {
    font-family: 'Cormorant Garamond', serif;
    font-size: 22px; font-weight: 400;
  }
  .cart-close {
    background: none; border: none; cursor: none; font-size: 20px;
    color: var(--mid-gray); line-height: 1;
  }
  .cart-items { flex: 1; overflow-y: auto; padding: 24px 32px; }
  .cart-empty {
    text-align: center; padding: 60px 0;
    color: var(--mid-gray); font-size: 13px; line-height: 2;
  }
  .cart-empty .cart-empty-icon { font-size: 48px; margin-bottom: 16px; display: block; }
  .cart-item {
    display: flex; gap: 16px; padding: 20px 0;
    border-bottom: 1px solid var(--border);
    animation: fadeUp 0.4s ease;
  }
  .cart-item-img {
    width: 70px; height: 85px;
    background: var(--cream);
    flex-shrink: 0;
    display: flex; align-items: center; justify-content: center;
    font-size: 28px;
  }
  .cart-item-info { flex: 1; }
  .cart-item-name { font-size: 11px; letter-spacing: 1.5px; text-transform: uppercase; margin-bottom: 6px; }
  .cart-item-price { font-size: 13px; color: var(--accent); }
  .cart-item-qty { display: flex; align-items: center; gap: 10px; margin-top: 10px; }
  .qty-btn {
    width: 24px; height: 24px; border: 1px solid var(--border);
    background: none; cursor: none; font-size: 14px;
    display: flex; align-items: center; justify-content: center;
    transition: all 0.2s;
  }
  .qty-btn:hover { background: var(--dark); color: white; border-color: var(--dark); }
  .qty-val { font-size: 13px; min-width: 20px; text-align: center; }
  .cart-remove { background: none; border: none; cursor: none; color: var(--mid-gray); font-size: 11px; letter-spacing: 1px; text-transform: uppercase; margin-top: 8px; transition: color 0.2s; }
  .cart-remove:hover { color: #c0392b; }
  .cart-footer { padding: 24px 32px; border-top: 1px solid var(--border); }
  .cart-total { display: flex; justify-content: space-between; margin-bottom: 20px; font-size: 14px; }
  .cart-total strong { font-weight: 500; }
  .cart-checkout {
    width: 100%; padding: 16px;
    background: var(--dark); color: white; border: none; cursor: none;
    font-size: 11px; letter-spacing: 3px; text-transform: uppercase;
    font-family: 'DM Sans', sans-serif;
    transition: background 0.3s;
  }
  .cart-checkout:hover { background: var(--accent); }

  /* SEARCH OVERLAY */
  .search-overlay {
    position: fixed; inset: 0;
    background: rgba(250,250,248,0.97);
    z-index: 3000;
    display: flex; flex-direction: column; align-items: center; justify-content: flex-start;
    padding-top: 120px;
    opacity: 0; pointer-events: none;
    transition: opacity 0.3s;
    backdrop-filter: blur(20px);
  }
  .search-overlay.open { opacity: 1; pointer-events: all; }
  .search-close-btn {
    position: absolute; top: 28px; right: 48px;
    background: none; border: none; cursor: none;
    font-size: 24px; color: var(--mid-gray);
  }
  .search-input-wrap {
    width: 100%; max-width: 640px;
    border-bottom: 2px solid var(--dark);
    display: flex; align-items: center; padding: 0 0 12px;
    margin-bottom: 48px;
  }
  .search-input {
    flex: 1; font-size: 28px; border: none; outline: none;
    background: transparent; font-family: 'Cormorant Garamond', serif;
    color: var(--dark);
  }
  .search-input::placeholder { color: var(--warm-gray); }
  .search-icon-large { font-size: 22px; color: var(--mid-gray); margin-right: 12px; }
  .search-suggestions h4 {
    font-size: 10px; letter-spacing: 3px; text-transform: uppercase;
    color: var(--mid-gray); margin-bottom: 20px;
  }
  .search-tags { display: flex; flex-wrap: wrap; gap: 10px; }
  .search-tag {
    padding: 8px 18px; border: 1px solid var(--border);
    font-size: 11px; letter-spacing: 1.5px; text-transform: uppercase;
    color: var(--mid-gray); cursor: none;
    transition: all 0.2s;
    background: none; font-family: 'DM Sans', sans-serif;
  }
  .search-tag:hover { background: var(--dark); color: white; border-color: var(--dark); }

  /* SCROLL ANIMATIONS */
  .reveal {
    opacity: 0; transform: translateY(40px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }
  .reveal.visible { opacity: 1; transform: translateY(0); }
  .reveal-delay-1 { transition-delay: 0.1s; }
  .reveal-delay-2 { transition-delay: 0.2s; }
  .reveal-delay-3 { transition-delay: 0.3s; }

  /* TOAST */
  .toast {
    position: fixed; bottom: 32px; left: 50%; transform: translateX(-50%) translateY(80px);
    background: var(--dark); color: white;
    padding: 14px 28px; font-size: 12px; letter-spacing: 1.5px;
    z-index: 9990; transition: transform 0.4s ease;
    display: flex; align-items: center; gap: 10px;
    pointer-events: none;
  }
  .toast.show { transform: translateX(-50%) translateY(0); }

  /* MOOD VISUAL PLACEHOLDERS */
  .mood-day-visual {
    background: linear-gradient(135deg, #F7EDE2, #EDE9E1);
    display: flex; align-items: center; justify-content: center;
    font-size: 80px;
  }
  .mood-night-visual {
    background: linear-gradient(135deg, #1A1814, #2A2420);
    display: flex; align-items: center; justify-content: center;
    font-size: 80px;
  }

  /* Product placeholder images using CSS */
  .prod-emoji { font-size: 60px; display: flex; align-items: center; justify-content: center; height: 100%; }

  /* Hamburger for mobile */
  .hamburger { display: none; flex-direction: column; gap: 5px; cursor: none; background: none; border: none; }
  .hamburger span { display: block; width: 22px; height: 1.5px; background: var(--dark); transition: all 0.3s; }

  @media (max-width: 1100px) {
    .product-grid { grid-template-columns: repeat(3, 1fr); }
    .collections-grid { grid-template-columns: 1fr 1fr; }
    .collection-card:first-child { grid-row: auto; }
    .footer-top { grid-template-columns: 1fr 1fr; gap: 40px; }
  }
  @media (max-width: 768px) {
    nav { padding: 0 24px; }
    section { padding: 72px 24px; }
    .nav-links { display: none; }
    .hamburger { display: flex; }
    .product-grid { grid-template-columns: repeat(2, 1fr); }
    .collections-grid { grid-template-columns: 1fr; }
    .mood-section { grid-template-columns: 1fr; padding: 72px 24px; }
    .testimonials-grid { grid-template-columns: 1fr; }
    .footer-top { grid-template-columns: 1fr; }
    .cart-drawer { width: 100%; }
    .hero-cta { flex-direction: column; }
    .manifesto { padding: 80px 24px; }
    .footer-bottom { flex-direction: column; gap: 16px; }
  }
</style>
</head>
<body>

<!-- Custom Cursor -->
<div class="cursor" id="cursor"></div>
<div class="cursor-ring" id="cursorRing"></div>

<!-- Toast notification -->
<div class="toast" id="toast">✓ Added to cart</div>

<!-- Search Overlay -->
<div class="search-overlay" id="searchOverlay">
  <button class="search-close-btn" onclick="closeSearch()">✕</button>
  <div class="search-input-wrap">
    <span class="search-icon-large">⌕</span>
    <input class="search-input" type="text" placeholder="Search PILE STUDIO…" id="searchInput" autofocus>
  </div>
  <div class="search-suggestions" style="max-width:640px;width:100%">
    <h4>Popular Searches</h4>
    <div class="search-tags">
      <button class="search-tag" onclick="setSearch('Lighting')">Lighting</button>
      <button class="search-tag" onclick="setSearch('Wall Art')">Wall Art</button>
      <button class="search-tag" onclick="setSearch('Clocks')">Clocks</button>
      <button class="search-tag" onclick="setSearch('Rugs')">Rugs</button>
      <button class="search-tag" onclick="setSearch('Decor')">Decor</button>
      <button class="search-tag" onclick="setSearch('New Arrivals')">New Arrivals</button>
    </div>
  </div>
</div>

<!-- Cart Drawer -->
<div class="cart-overlay" id="cartOverlay" onclick="closeCart()"></div>
<div class="cart-drawer" id="cartDrawer">
  <div class="cart-header">
    <h3>Your Cart (<span id="cartCount">0</span>)</h3>
    <button class="cart-close" onclick="closeCart()">✕</button>
  </div>
  <div class="cart-items" id="cartItems">
    <div class="cart-empty">
      <span class="cart-empty-icon">🛒</span>
      Your cart is empty.<br>Discover our curated objects.
    </div>
  </div>
  <div class="cart-footer" id="cartFooter" style="display:none">
    <div class="cart-total">
      <span>Subtotal</span>
      <strong id="cartTotal">$0.00</strong>
    </div>
    <button class="cart-checkout">Proceed to Checkout →</button>
  </div>
</div>

<!-- Announcement Bar -->
<div class="announcement">
  <div class="announcement-ticker">
    NEW ARRIVALS DROPPING WEEKLY &nbsp;&nbsp;&nbsp;✦&nbsp;&nbsp;&nbsp; FREE SHIPPING ON ORDERS OVER $120 &nbsp;&nbsp;&nbsp;✦&nbsp;&nbsp;&nbsp; PILE STUDIO — OBJECTS WITH INTENTION &nbsp;&nbsp;&nbsp;✦&nbsp;&nbsp;&nbsp; STUDIO SALE — UP TO 40% OFF SELECT PIECES &nbsp;&nbsp;&nbsp;✦&nbsp;&nbsp;&nbsp;
    NEW ARRIVALS DROPPING WEEKLY &nbsp;&nbsp;&nbsp;✦&nbsp;&nbsp;&nbsp; FREE SHIPPING ON ORDERS OVER $120 &nbsp;&nbsp;&nbsp;✦&nbsp;&nbsp;&nbsp; PILE STUDIO — OBJECTS WITH INTENTION
  </div>
</div>

<!-- Navigation -->
<nav id="mainNav">
  <a href="#" class="nav-logo">PILE STUDIO</a>
  <ul class="nav-links">
    <li><a href="#collections">Collections</a></li>
    <li><a href="#products">Shop All</a></li>
    <li><a href="#mood">Lighting</a></li>
    <li><a href="#about">About</a></li>
    <li><a href="#contact">Contact</a></li>
  </ul>
  <div class="nav-actions">
    <button class="nav-icon-btn" onclick="openSearch()" title="Search">⌕</button>
    <button class="nav-icon-btn" title="Account">○</button>
    <button class="nav-icon-btn" onclick="openCart()" title="Cart" style="position:relative">
      ◻
      <span class="cart-badge" id="cartBadge" style="display:none">0</span>
    </button>
    <button class="hamburger" id="hamburger" onclick="toggleMenu()">
      <span></span><span></span><span></span>
    </button>
  </div>
</nav>

<!-- HERO -->
<section class="hero">
  <div class="hero-bg"></div>
  <div class="hero-noise"></div>
  <div class="hero-content">
    <p class="hero-eyebrow">Objects with Intention</p>
    <h1 class="hero-title">
      Design<br><em>your</em><br>space.
    </h1>
    <p class="hero-sub">Curated home objects that carry weight, meaning, and presence.</p>
    <div class="hero-cta">
      <a href="#products" class="btn-primary">Shop Now</a>
      <a href="#collections" class="btn-outline">Explore Collections</a>
    </div>
  </div>
  <div class="hero-scroll">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<!-- MARQUEE -->
<div class="marquee-strip">
  <div class="marquee-track">
    <span>PILE STUDIO</span><span class="dot">✦</span>
    <span>OBJECTS WITH INTENTION</span><span class="dot">✦</span>
    <span>LIGHTING</span><span class="dot">✦</span>
    <span>HOME ACCESSORIES</span><span class="dot">✦</span>
    <span>WALL ART</span><span class="dot">✦</span>
    <span>CURATED DECOR</span><span class="dot">✦</span>
    <span>PILE STUDIO</span><span class="dot">✦</span>
    <span>OBJECTS WITH INTENTION</span><span class="dot">✦</span>
    <span>LIGHTING</span><span class="dot">✦</span>
    <span>HOME ACCESSORIES</span><span class="dot">✦</span>
    <span>WALL ART</span><span class="dot">✦</span>
    <span>CURATED DECOR</span><span class="dot">✦</span>
  </div>
</div>

<!-- COLLECTIONS -->
<section class="collections" id="collections">
  <div class="collections-header reveal">
    <div>
      <p class="section-label">Browse</p>
      <h2 class="section-title">Collections</h2>
    </div>
    <a href="#products" class="btn-outline">View All →</a>
  </div>
  <div class="collections-grid">
    <div class="collection-card reveal">
      <div class="collection-img" style="height:100%;min-height:480px;background:linear-gradient(145deg,#E8DED0,#C8BFB2);display:flex;align-items:center;justify-content:center;font-size:120px;">🕯️</div>
      <div class="collection-overlay">
        <p class="collection-name">Lighting</p>
        <a href="#products" class="collection-link">Explore → </a>
      </div>
    </div>
    <div class="collection-card reveal reveal-delay-1">
      <div class="collection-img" style="aspect-ratio:4/5;background:linear-gradient(145deg,#E0D8CC,#D4CCC0);display:flex;align-items:center;justify-content:center;font-size:80px;">🖼️</div>
      <div class="collection-overlay">
        <p class="collection-name">Wall Art</p>
        <a href="#products" class="collection-link">Explore →</a>
      </div>
    </div>
    <div class="collection-card reveal reveal-delay-2">
      <div class="collection-img" style="aspect-ratio:4/5;background:linear-gradient(145deg,#D8D0C8,#CCB8A8);display:flex;align-items:center;justify-content:center;font-size:80px;">🪴</div>
      <div class="collection-overlay">
        <p class="collection-name">Accessories</p>
        <a href="#products" class="collection-link">Explore →</a>
      </div>
    </div>
    <div class="collection-card reveal reveal-delay-1">
      <div class="collection-img" style="aspect-ratio:4/5;background:linear-gradient(145deg,#D0C8C0,#C4B8AC);display:flex;align-items:center;justify-content:center;font-size:80px;">🪑</div>
      <div class="collection-overlay">
        <p class="collection-name">Furniture</p>
        <a href="#products" class="collection-link">Explore →</a>
      </div>
    </div>
    <div class="collection-card reveal reveal-delay-2">
      <div class="collection-img" style="aspect-ratio:4/5;background:linear-gradient(145deg,#C8C0B8,#BEB4A8);display:flex;align-items:center;justify-content:center;font-size:80px;">🪞</div>
      <div class="collection-overlay">
        <p class="collection-name">Objects</p>
        <a href="#products" class="collection-link">Explore →</a>
      </div>
    </div>
  </div>
</section>

<!-- PRODUCTS -->
<section class="products" id="products">
  <div class="reveal" style="margin-bottom:0">
    <p class="section-label">Curated picks</p>
    <h2 class="section-title" style="margin-bottom:36px">Best Sellers</h2>
  </div>
  <div class="product-filters reveal">
    <button class="filter-btn active" onclick="filterProducts('all', this)">All</button>
    <button class="filter-btn" onclick="filterProducts('lighting', this)">Lighting</button>
    <button class="filter-btn" onclick="filterProducts('art', this)">Wall Art</button>
    <button class="filter-btn" onclick="filterProducts('accessories', this)">Accessories</button>
    <button class="filter-btn" onclick="filterProducts('objects', this)">Objects</button>
  </div>
  <div class="product-grid" id="productGrid">
    <!-- Products injected by JS -->
  </div>
</section>

<!-- DAY / NIGHT MOOD -->
<section class="mood-section" id="mood">
  <div class="mood-text">
    <p class="section-label">Atmosphere</p>
    <h2 class="section-title reveal">Statement<br>by day,<br><em>Mood</em> by night.</h2>
    <p class="reveal reveal-delay-1">Designed to catch the eye in sunlight and calm the mind after dark. Every piece speaks two languages.</p>
    <div class="mood-toggle-wrap reveal reveal-delay-2">
      <button class="mood-btn active" id="dayBtn" onclick="setMood('day')">☀ Day</button>
      <button class="mood-btn" id="nightBtn" onclick="setMood('night')">☽ Night</button>
    </div>
    <a href="#products" class="btn-primary reveal reveal-delay-3">Shop Lighting</a>
  </div>
  <div class="mood-img-wrap reveal reveal-delay-1">
    <div class="mood-img day mood-day-visual" id="dayImg">🕯️</div>
    <div class="mood-img night mood-night-visual" id="nightImg">🌙</div>
  </div>
</section>

<!-- MANIFESTO -->
<section class="manifesto" id="about">
  <div class="manifesto-bg-text">PILE</div>
  <div class="manifesto-content">
    <p class="section-label reveal" style="justify-content:center">Our Philosophy</p>
    <blockquote class="reveal reveal-delay-1">
      "A home is not decorated. It is curated — one intentional object at a time."
    </blockquote>
    <cite class="reveal reveal-delay-2">— Pile Studio, est. 2024</cite>
    <p class="reveal reveal-delay-2" style="color:var(--mid-gray);font-size:14px;margin-top:36px;line-height:1.9;font-weight:300;max-width:520px;margin-left:auto;margin-right:auto">
      We believe the objects you live with should earn their place. Each piece in our collection is selected for its character, craft, and the quiet beauty it brings to everyday moments.
    </p>
  </div>
</section>

<!-- BANNER -->
<div class="banner" id="bannerSection">
  <div class="banner-bg" id="bannerBg"></div>
  <div class="banner-inner">
    <p class="banner-eyebrow reveal">New Arrivals</p>
    <h2 class="banner-title reveal reveal-delay-1">Life is<br>in the details.</h2>
    <p class="banner-sub reveal reveal-delay-2">The pieces you interact with every day matter most. Build your home around them.</p>
    <a href="#products" class="btn-primary reveal reveal-delay-2" id="contact">Shop Accessories →</a>
  </div>
</div>

<!-- TESTIMONIALS -->
<section class="testimonials">
  <div class="reveal">
    <p class="section-label">Community</p>
    <h2 class="section-title">What our<br><em>customers</em> say</h2>
  </div>
  <div class="testimonials-grid">
    <div class="testimonial-card reveal">
      <div class="stars">★★★★★</div>
      <p class="testimonial-text">"The lamp transformed my study. It's the first thing guests notice — and ask about."</p>
      <span class="testimonial-author">— Mia K., Singapore</span>
    </div>
    <div class="testimonial-card reveal reveal-delay-1">
      <div class="stars">★★★★★</div>
      <p class="testimonial-text">"Pile Studio pieces feel like they were designed specifically for my home. Impeccable quality."</p>
      <span class="testimonial-author">— Jordan T., London</span>
    </div>
    <div class="testimonial-card reveal reveal-delay-2">
      <div class="stars">★★★★★</div>
      <p class="testimonial-text">"Fast shipping, beautiful packaging, and the piece itself is even better in person."</p>
      <span class="testimonial-author">— Ren A., Tokyo</span>
    </div>
  </div>
</section>

<!-- NEWSLETTER -->
<section class="newsletter">
  <p class="section-label">Stay in the loop</p>
  <h2 class="section-title reveal">Be part of<br>PILE STUDIO.</h2>
  <p class="reveal reveal-delay-1">Early access to new collections and exclusive drops.<br>No noise. Only beautiful things.</p>
  <div class="newsletter-form reveal reveal-delay-2">
    <input type="email" placeholder="your@email.com" id="emailInput">
    <button onclick="subscribeNewsletter()">Subscribe</button>
  </div>
</section>

<!-- FOOTER -->
<footer>
  <div class="footer-top">
    <div>
      <div class="footer-brand-name">PILE STUDIO</div>
      <p class="footer-tagline">Curated home objects designed to earn<br>their place in your space. Every day.</p>
      <div class="footer-social">
        <a href="#" class="social-link" title="Instagram">◻</a>
        <a href="#" class="social-link" title="TikTok">▷</a>
        <a href="#" class="social-link" title="Pinterest">◇</a>
      </div>
    </div>
    <div class="footer-col">
      <h4>Shop</h4>
      <ul>
        <li><a href="#">All Products</a></li>
        <li><a href="#">Lighting</a></li>
        <li><a href="#">Wall Art</a></li>
        <li><a href="#">Accessories</a></li>
        <li><a href="#">Sale</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Info</h4>
      <ul>
        <li><a href="#">Our Story</a></li>
        <li><a href="#">FAQ</a></li>
        <li><a href="#">Contact</a></li>
        <li><a href="#">Track Order</a></li>
      </ul>
    </div>
    <div class="footer-col">
      <h4>Policies</h4>
      <ul>
        <li><a href="#">Privacy Policy</a></li>
        <li><a href="#">Refund Policy</a></li>
        <li><a href="#">Shipping</a></li>
        <li><a href="#">Terms of Service</a></li>
      </ul>
    </div>
  </div>
  <div class="footer-bottom">
    <span class="footer-copy">© 2026 PILE STUDIO. All rights reserved.</span>
    <div class="footer-legal">
      <a href="#">Privacy</a>
      <a href="#">Terms</a>
      <a href="#">Cookies</a>
    </div>
  </div>
</footer>

<script>
// ——— CURSOR ———
const cursor = document.getElementById('cursor');
const ring = document.getElementById('cursorRing');
let mx = 0, my = 0, rx = 0, ry = 0;

document.addEventListener('mousemove', e => {
  mx = e.clientX; my = e.clientY;
  cursor.style.transform = `translate(${mx - 6}px, ${my - 6}px)`;
});

function animRing() {
  rx += (mx - rx) * 0.12;
  ry += (my - ry) * 0.12;
  ring.style.transform = `translate(${rx - 18}px, ${ry - 18}px)`;
  requestAnimationFrame(animRing);
}
animRing();

document.querySelectorAll('a, button, .collection-card, .product-card, .testimonial-card').forEach(el => {
  el.addEventListener('mouseenter', () => ring.classList.add('expand'));
  el.addEventListener('mouseleave', () => ring.classList.remove('expand'));
});

// ——— PRODUCTS DATA ———
const products = [
  { id: 1, name: 'Dome Table Lamp', price: 89, originalPrice: 120, badge: 'Sale', emoji: '🔦', category: 'lighting' },
  { id: 2, name: 'Geometric Clock', price: 65, originalPrice: null, badge: null, emoji: '🕐', category: 'accessories' },
  { id: 3, name: 'Linen Wall Print', price: 42, originalPrice: 60, badge: 'Sale', emoji: '🎨', category: 'art' },
  { id: 4, name: 'Rattan Pendant', price: 110, originalPrice: null, badge: 'New', emoji: '💡', category: 'lighting' },
  { id: 5, name: 'Ceramic Vase', price: 55, originalPrice: null, badge: null, emoji: '🏺', category: 'objects' },
  { id: 6, name: 'Arc Floor Lamp', price: 195, originalPrice: 240, badge: 'Sale', emoji: '🕯️', category: 'lighting' },
  { id: 7, name: 'Abstract Poster', price: 38, originalPrice: null, badge: null, emoji: '🖼️', category: 'art' },
  { id: 8, name: 'Woven Wall Basket', price: 72, originalPrice: null, badge: 'New', emoji: '🧺', category: 'accessories' },
];

let activeFilter = 'all';
let cartItems = [];

function renderProducts(filter = 'all') {
  const grid = document.getElementById('productGrid');
  const filtered = filter === 'all' ? products : products.filter(p => p.category === filter);
  grid.innerHTML = '';
  filtered.forEach((p, i) => {
    const card = document.createElement('div');
    card.className = 'product-card';
    card.style.transitionDelay = `${i * 0.07}s`;
    card.innerHTML = `
      <div class="product-img-wrap">
        <div class="product-img prod-emoji">${p.emoji}</div>
        ${p.badge ? `<span class="product-badge">${p.badge}</span>` : ''}
        <button class="product-wishlist" onclick="wishlist(event,'${p.name}')">♡</button>
        <button class="product-quick-add" onclick="addToCart(event,${p.id})">+ Add to Cart</button>
      </div>
      <div class="product-info">
        <p class="product-name">${p.name}</p>
        <p class="product-price">
          ${p.originalPrice ? `<span class="sale">$${p.price}</span><span class="original">$${p.originalPrice}</span>` : `$${p.price}`}
        </p>
      </div>
    `;
    grid.appendChild(card);
    requestAnimationFrame(() => {
      setTimeout(() => card.classList.add('visible'), 50 + i * 60);
    });
  });
}

function filterProducts(filter, btn) {
  activeFilter = filter;
  document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
  btn.classList.add('active');
  const grid = document.getElementById('productGrid');
  grid.style.opacity = '0';
  grid.style.transform = 'translateY(10px)';
  grid.style.transition = 'opacity 0.3s, transform 0.3s';
  setTimeout(() => {
    renderProducts(filter);
    grid.style.opacity = '1';
    grid.style.transform = 'translateY(0)';
  }, 300);
}

// ——— CART ———
function addToCart(e, id) {
  e.stopPropagation();
  const product = products.find(p => p.id === id);
  const existing = cartItems.find(i => i.id === id);
  if (existing) {
    existing.qty++;
  } else {
    cartItems.push({ ...product, qty: 1 });
  }
  updateCartUI();
  showToast(`✓ ${product.name} added`);
}

function updateCartUI() {
  const count = cartItems.reduce((s, i) => s + i.qty, 0);
  const badge = document.getElementById('cartBadge');
  const countEl = document.getElementById('cartCount');
  badge.style.display = count > 0 ? 'flex' : 'none';
  badge.textContent = count;
  countEl.textContent = count;

  const container = document.getElementById('cartItems');
  const footer = document.getElementById('cartFooter');
  if (cartItems.length === 0) {
    container.innerHTML = `<div class="cart-empty"><span class="cart-empty-icon">🛒</span>Your cart is empty.<br>Discover our curated objects.</div>`;
    footer.style.display = 'none';
    return;
  }
  footer.style.display = 'block';
  container.innerHTML = cartItems.map(item => `
    <div class="cart-item">
      <div class="cart-item-img">${item.emoji}</div>
      <div class="cart-item-info">
        <p class="cart-item-name">${item.name}</p>
        <p class="cart-item-price">$${item.price}</p>
        <div class="cart-item-qty">
          <button class="qty-btn" onclick="changeQty(${item.id},-1)">−</button>
          <span class="qty-val">${item.qty}</span>
          <button class="qty-btn" onclick="changeQty(${item.id},1)">+</button>
        </div>
        <button class="cart-remove" onclick="removeItem(${item.id})">Remove</button>
      </div>
    </div>
  `).join('');
  const total = cartItems.reduce((s, i) => s + i.price * i.qty, 0);
  document.getElementById('cartTotal').textContent = `$${total.toFixed(2)}`;
}

function changeQty(id, delta) {
  const item = cartItems.find(i => i.id === id);
  if (!item) return;
  item.qty += delta;
  if (item.qty <= 0) cartItems = cartItems.filter(i => i.id !== id);
  updateCartUI();
}
function removeItem(id) {
  cartItems = cartItems.filter(i => i.id !== id);
  updateCartUI();
}
function wishlist(e, name) {
  e.stopPropagation();
  e.target.textContent = '♥';
  e.target.style.color = '#c0392b';
  showToast(`♥ ${name} saved to wishlist`);
}

function openCart() {
  document.getElementById('cartDrawer').classList.add('open');
  document.getElementById('cartOverlay').classList.add('open');
  document.body.style.overflow = 'hidden';
}
function closeCart() {
  document.getElementById('cartDrawer').classList.remove('open');
  document.getElementById('cartOverlay').classList.remove('open');
  document.body.style.overflow = '';
}

// ——— SEARCH ———
function openSearch() {
  document.getElementById('searchOverlay').classList.add('open');
  document.body.style.overflow = 'hidden';
  setTimeout(() => document.getElementById('searchInput').focus(), 300);
}
function closeSearch() {
  document.getElementById('searchOverlay').classList.remove('open');
  document.body.style.overflow = '';
}
function setSearch(val) {
  document.getElementById('searchInput').value = val;
  document.getElementById('searchInput').focus();
}
document.addEventListener('keydown', e => {
  if (e.key === 'Escape') { closeSearch(); closeCart(); }
});

// ——— MOOD TOGGLE ———
function setMood(mode) {
  const dayImg = document.getElementById('dayImg');
  const nightImg = document.getElementById('nightImg');
  const dayBtn = document.getElementById('dayBtn');
  const nightBtn = document.getElementById('nightBtn');
  if (mode === 'night') {
    dayImg.classList.add('hide');
    nightImg.classList.add('show');
    dayBtn.classList.remove('active');
    nightBtn.classList.add('active');
  } else {
    dayImg.classList.remove('hide');
    nightImg.classList.remove('show');
    dayBtn.classList.add('active');
    nightBtn.classList.remove('active');
  }
}

// ——— TOAST ———
let toastTimer;
function showToast(msg) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.classList.add('show');
  clearTimeout(toastTimer);
  toastTimer = setTimeout(() => t.classList.remove('show'), 2500);
}

// ——— NEWSLETTER ———
function subscribeNewsletter() {
  const val = document.getElementById('emailInput').value;
  if (!val || !val.includes('@')) {
    showToast('Please enter a valid email');
    return;
  }
  document.getElementById('emailInput').value = '';
  showToast('✓ Welcome to PILE STUDIO!');
}

// ——— SCROLL REVEAL ———
const revealEls = document.querySelectorAll('.reveal, .product-card');
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('visible');
    }
  });
}, { threshold: 0.1 });
revealEls.forEach(el => observer.observe(el));

// ——— STICKY NAV ———
window.addEventListener('scroll', () => {
  const nav = document.getElementById('mainNav');
  if (window.scrollY > 60) nav.classList.add('scrolled');
  else nav.classList.remove('scrolled');

  // Parallax on banner
  const banner = document.getElementById('bannerSection');
  const bannerBg = document.getElementById('bannerBg');
  if (banner && bannerBg) {
    const rect = banner.getBoundingClientRect();
    const offset = rect.top * 0.3;
    bannerBg.style.transform = `translateY(${offset}px)`;
  }
});

// ——— INIT ———
renderProducts();
updateCartUI();
</script>
</body>
</html>
